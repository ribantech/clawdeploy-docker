# Copy to .env and fill for local testing. Inngest/Hetzner will set these per tenant.

# Telegram (from getsetclaw per user)
TELEGRAM_ENABLED=true
TELEGRAM_BOT_TOKEN=123456:ABC-def...
TELEGRAM_ALLOW_FROM=*
# TELEGRAM_DM_POLICY=pairing

# Model + API key (per tenant; OpenClaw uses these for agents.defaults.model and env.vars)
OPENCLAW_MODEL_ID=anthropic/claude-sonnet-4-5
OPENCLAW_MODEL_ENV_KEY=ANTHROPIC_API_KEY
OPENCLAW_MODEL_API_KEY=sk-ant-...
# Optional: custom provider (e.g. OpenRouter)
# OPENCLAW_MODEL_PROVIDER_ID=openrouter
# OPENCLAW_MODEL_BASE_URL=https://openrouter.ai/api/v1

# Optional: extra channels as JSON object merged into config.channels (e.g. Discord, Slack)
# OPENCLAW_AGENTS_JSON={"discord":{"enabled":true,"token":"..."}}
